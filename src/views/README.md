# Presentation layer

The prototype views currently compose in `src/App.jsx` over the shared twin snapshot. This directory is reserved for extracting Operations, Investigation, and Management renderers without changing the store contract.
